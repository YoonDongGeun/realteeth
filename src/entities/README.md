# Entities Layer

비즈니스 엔티티를 정의하고 관리합니다.

## 역할
- 비즈니스 도메인의 핵심 엔티티 정의
- 엔티티의 타입, 스키마, 기본 동작 정의
- 데이터 모델과 관련된 비즈니스 규칙 포함

## 의존성 규칙
- ✅ shared에만 의존 가능
- ❌ 다른 entities에 의존 불가
- ❌ features, widgets, app 레이어에 의존 불가

## 예시
- `weather/` - 날씨 데이터 모델, API 인터페이스, 상태 관리
- `city/` - 도시 데이터 모델, 위치 정보
